---
title: Projects by Client
---
{% assign current_projects = site.projects | where_exp:"project", "project.end > 2016" %}
{% assign current_usaid = current_projects | where_exp: "project", "project.client contains 'U.S. Agency for International Development'" %}{% assign current_dfid = current_projects | where_exp: "project", "project.client contains 'U.K. Department for International Development'" %}{% assign current_ec = current_projects | where_exp: "project", "project.client contains 'European Commission'" %}{% assign current_ebrd = current_projects | where_exp: "project", "project.client contains 'European Bank for Reconstruction and Development'" %}{% assign usaid_projects = site.projects | where_exp: "project", "project.client contains 'U.S. Agency for International Development'" %}{% assign dfid_projects = site.projects | where_exp: "project", "project.client contains 'U.K. Department for International Development'" %}{% assign ec_projects = site.projects | where_exp: "project", "project.client contains 'European Commission'" %}{% assign ebrd_projects = site.projects | where_exp: "project", "project.client contains 'European Bank for Reconstruction and Development'" %}[
  [
    [
      "Clients",
      "All Projects",
      "Currently Active Projects"
    ],
    [
      "USAID",
      {{ usaid_projects.size }},
      {{ current_usaid.size }}
    ],
    [
      "DFID",
      {{ dfid_projects.size }},
      {{ current_dfid.size }}
    ],
    [
      "EC",
      {{ ec_projects.size }},
      {{ current_ec.size }}
    ],
    [
      "EBRD",
      {{ ebrd_projects.size }},
      {{ current_ebrd.size }}
    ]
  ]
]
