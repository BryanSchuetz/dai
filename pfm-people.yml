---
---
{% assign pfm_experts = site.experts | where_exp:"expert", "expert.solutions contains 'Public Financial Management'" %}
{% for expert in pfm_experts %}
- title: "{{ expert.title }}"
  lastname: "{{ expert.lastname }}"
  summary: "{{ expert.summary }}"
  job-title: "{{ expert.job-title }}"
  immage: "{{ expert.image }}"
  body: "{{ expert.content }}"
{% endfor %}